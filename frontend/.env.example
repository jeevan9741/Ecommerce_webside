# Copy to .env for local development. In production, set these in Vercel → Settings → Environment Variables.
# NEXT_PUBLIC_* values are baked in at build time — redeploy after changing them.

# Express backend (no trailing slash). Production: https://ecommerce-training-academy-backend.onrender.com
NEXT_PUBLIC_API_URL=http://localhost:5000
# This site's public URL. Production: https://ecommerce-training-academy-jeevan-f45f.vercel.app
NEXT_PUBLIC_SITE_URL=http://localhost:3000
